# Deprecated local bundle values example. Canonical Sugarkube deployments should
# use the values maintained in the Sugarkube repo and an immutable GHCR image tag.

image:
  repository: ghcr.io/futuroptimist/tokenplace-relay
  tag: main-latest

env:
  TOKEN_PLACE_ENV: production

nodeSelector:
  kubernetes.io/arch: arm64
