{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "tokenplace-relay.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "tokenplace-relay.labels" . | nindent 4 }}
{{- end }}
