# Legacy local values example. Copy only for disposable bundle-hook experiments.
# For Sugarkube staging/prod, use the Sugarkube repo's token.place values and
# deploy with: just tokenplace-oci-deploy env=staging tag=main-REPLACE_SHORTSHA

image:
  repository: ghcr.io/futuroptimist/tokenplace-relay
  tag: main-latest

env:
  TOKEN_PLACE_ENV: production

nodeSelector:
  kubernetes.io/arch: arm64
