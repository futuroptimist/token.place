# Legacy Sugarkube bundle definition retained for local experiments.
# Canonical staging/prod deploys run from the Sugarkube repo with:
#   just tokenplace-oci-deploy env=staging tag=main-REPLACE_SHORTSHA
RELEASE=tokenplace
CHART=oci://ghcr.io/futuroptimist/charts/tokenplace
VALUES_FILE=/opt/sugarkube/helm-values/token-place-values.yaml
NAMESPACE=tokenplace
WAIT_TARGETS=deployment.apps/tokenplace
NOTES=Deploy the token.place relay from the published GHCR OCI chart and image tag.
